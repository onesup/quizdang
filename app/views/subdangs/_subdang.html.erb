<div class="splash card" style="background-image:url(<%= subdang.featured_image.url(:regular) %>)">
  <div class="island">
    <h1 class="splash--title">
      <%= link_to subdang do %>
        <%= subdang.name %>
      <% end %>
    </h1>

    <h2 class="splash--subtitle">
      <% subdang.children.each do |child| %>
        <%= link_to child.name, child %>
      <% end %>
    </h2>
  </div>
</div>
